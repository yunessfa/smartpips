# گزارش تمیزکاری — شهریور ۱۴۰۵ (۲۰۲۶-۰۹)

> **قاعده‌ای که رعایت شد:** هیچ چیزی حذف نشد. هر کد غیرفعال، **کامنت** شده و با
> برچسب `DISABLED 2026-09` یا `[LEGACY]` مشخص است تا بتوانی با برداشتن کامنت
> برگردانی‌اش.
>
> **دست‌نخورده:** پوشه‌ی `backend/apps/strategy/` (استراتژی اصلی) به هیچ عنوان
> ویرایش نشد — نه `engine`، نه `decision`، نه `rule_engine`، نه `levels`،
> نه `orderflow`، نه `regime`، نه `backtest`، نه `presets`، نه `exit_engine`.
> تنها فایل استراتژی که خوانده شد `risk.py` بود و آن هم فقط **خوانده** شد
> (تابع `daily_risk_guard` از بیرون صدا زده می‌شود، خودش تغییر نکرد).

---

## ۱) بیت‌یونیکس — کامنت شد، حذف نشد

### کلید اصلی خاموش‌کردن
`backend/config/urls.py` — کل مسیر API بیت‌یونیکس کامنت شد:
```python
# path("api/bitunix/", include("apps.bitunix.urls")),
```
با برداشتن کامنت این **یک خط**، کل بک‌اند بیت‌یونیکس برمی‌گردد.

### چرا `apps.bitunix` در `INSTALLED_APPS` ماند؟
عمدی است. جدول‌های `trades` و `alerts` به مایگریشن‌های بیت‌یونیکس وابسته‌اند
(`trades/migrations/0002_fix_bitunix_mirror_size.py`، `0003_…`، `0006_merge_…`،
`alerts/migrations/0002_…`). اگر اپ را از `INSTALLED_APPS` برداری،
`migrate` می‌شکند و دیتابیس بالا نمی‌آید. مسیر URL کامنت‌شده کافی است.

### فایل‌های تغییر یافته
| فایل | چه شد |
|---|---|
| `backend/config/settings.py` | کلیدهای `BITUNIX_*` کامنت و به `""` ست شدند؛ `CRYPTO_PERP_DATA_SOURCE` از `bitunix` به `binance` |
| `backend/config/urls.py` | مسیر `api/bitunix/` کامنت |
| `backend/apps/market/candles.py` | `_bitunix_candles` خط‌به‌خط کامنت؛ شاخه‌ی کریپتو هم کامنت |
| `backend/apps/bitunix/views.py` | فقط `except Exception` خفه‌شده در `_journal_open` به `log.exception` تبدیل شد (بقیه دست‌نخورده) |
| `frontend/src/App.jsx` | `import Bitunix` و `<Route path="/bitunix">` کامنت |
| `frontend/src/components/Layout.jsx` | آیتم منوی بیت‌یونیکس کامنت |
| `frontend/src/components/TradingAccountPanel.jsx` | مستقیم `PositionsPanel` را نشان می‌دهد؛ کل نسخه‌ی دوتب‌ی قبلی به‌صورت بلوک کامنت در انتهای فایل نگه داشته شد |
| `frontend/src/components/OrderBook.jsx` | عمق بازار از `lbankApi.futuresOrderbook` — **نکته:** توضیح بالای این فایل همیشه می‌گفت LBank ولی کد بیت‌یونیکس را صدا می‌زد؛ حالا واقعاً LBank است |
| `frontend/src/pages/Dashboard.jsx` | پنل `PositionChart` (وابسته به پوزیشن و کندل بیت‌یونیکس) کامنت |
| `frontend/src/pages/Scalp.jsx` | پر شدن خودکار موجودی از کیف‌پول بیت‌یونیکس کامنت؛ سایز‌گیری الان فقط دستی است |
| `frontend/src/components/BitunixMiniPanel.jsx`، `frontend/src/pages/Bitunix.jsx` | **بایت‌به‌بایت دست‌نخورده**، فقط یک سرتیتر «UNREACHABLE» گرفتند. دیگر هیچ‌جا import نمی‌شوند |

### یک موردی که آگاهانه فعال ماند
`frontend/src/components/TradeConfirmModal.jsx` هنوز `bitunixApi.placeOrder` را
صدا می‌زند و از `OrderTicket.jsx` در صفحه‌ی اسکالپ استفاده می‌شود.
این را **کامنت نکردم** چون `LBankFuturesClient.place_order` در بک‌اند عمداً
`LBankError` می‌اندازد؛ اگر این را هم می‌بستم، هیچ راه ثبت سفارشی باقی نمی‌ماند.
هر وقت گفتی، یا کامنتش می‌کنم یا به LBank وصلش می‌کنم.

> `bitunixApi` در `frontend/src/api/client.js` **عمداً export شده می‌ماند** —
> ۶ ماژول آن را import می‌کنند و در Vite یک named export گمشده خطای build
> است، نه اخطار.

---

## ۲) صفحه‌ی اسکالپ — تمیزتر

- **پنل بیت‌یونیکس حذف (کامنت) شد** — `PositionChart` و پنل حساب.
- **بخش بک‌تست پایین صفحه کامنت شد.** تابع `BacktestPanel` (خط ~۱۰۲۷) کامل در
  فایل ماند، فقط دیگر رندر نمی‌شود.
- **نوار معاملات باز (`OpenTradesBar`)** — کامپوننت جدید:
  - در **موبایل** فقط وقتی معامله‌ی بازی هست ظاهر می‌شود، `fixed` و چسبیده به
    پایین بالای نوار منو (`bottom-14`)، با `z-40` و
    `paddingBottom: env(safe-area-inset-bottom)` تا روی آیفون زیر نوار خانه نرود.
  - حالت بسته: یک نقطه‌ی چشمک‌زن + تعداد معامله + سود/زیان کل. با زدنش باز
    می‌شود (`max-h-[65vh] overflow-y-auto`) تا لیست کامل را ببینی.
  - در **دسکتاپ** همان پنل معمولی است (`hidden md:block`) — نواری‌کردن روی
    مانیتور بزرگ فایده‌ای ندارد.
  - هر ۲۰ ثانیه `tradesApi.list()` را می‌خواند.
- `pb-36 md:pb-6` به کانتینر اسکرول اضافه شد تا نوار جدید محتوای پایین صفحه را
  نپوشاند.

---

## ۳) اندیکاتورهای چارت بالای اسکالپ

چارت بالایی از `PositionChart` به `SignalChart` عوض شد و اندیکاتورها به آن
پاس داده می‌شوند:
```js
const SCALP_STUDIES = indicatorsToStudies([
  { key: "ema" }, { key: "rsi" }, { key: "macd" }, { key: "stoch" },
]);
```
**چرا لیست ثابت و نه خواندن از خروجی `computeScalp()`؟**
چون `indicators` که `lib/scalp.js` برمی‌گرداند برچسب‌های انسانی است
(`"EMA 9"`, `"Stoch %K/%D"`, `"Composite score"`) و `STUDY_MAP` در
`TradingView.jsx` با کلید کار می‌کند. تبدیل برچسب به کلید با regex شکننده
می‌شد؛ لیست کلیدها صریح و قابل‌اتکاست.

---

## ۴) طلا و نقره از LBank

**اول جست‌وجو شد و نتیجه مثبت بود** — پس مطابق دستورت جلو رفتم:
- `GOLD(XAU)USDT Perp` — تیک `0.01`، حداقل سفارش `5 USDT`، مارجین `%20`
- `SILVER(XAG)USDT Perp` — موجود در بازار فیوچرز فلزات LBank

### فایل جدید: `backend/apps/lbank/metals.py`
چون سندباکس اینترنت ندارد، **نام دقیق سیمبل و اندپوینت کندل را حدس نزدم** —
کد در زمان اجرا کشف می‌کند:
- `resolve_symbol()` روی `pub/instrument` می‌گردد و `XAU`/`GOLD` و `XAG`/`SILVER`
  را تطبیق می‌دهد؛ نتیجه ۶ ساعت کش می‌شود (نتیجه‌ی منفی هم کش می‌شود).
- `_KLINE_ATTEMPTS` چهار ترکیب اندپوینت/پارامتر را امتحان می‌کند و ترکیب موفق
  را ۲۴ ساعت کش می‌کند تا بار بعد اول همان را بزند.
- اگر کندل نیامد، `None` برمی‌گرداند (نه لیست خالی) و ۳۰ ثانیه کش منفی می‌گذارد.

### قیمت و کندل
| فایل | چه شد |
|---|---|
| `backend/apps/market/metals.py` | `_from_lbank()` اضافه شد؛ `_from_gold_api`، `_from_itick`، `_from_ctrader` با `[LEGACY]` کامنت‌گذاری شدند؛ `fetch_metal_price()` روی `METALS_DATA_SOURCE` شاخه می‌زند |
| `backend/apps/market/candles.py` | شاخه‌ی فلزات **بالای** `fetch_candles` قرار گرفت و بی‌قید‌وشرط return می‌کند؛ بلوک Twelve Data به `_twelvedata_candles()` منتقل و `[LEGACY]` شد |

**مهم:** `METALS_DATA_SOURCE="lbank"` (پیش‌فرض) عمداً **fallback بین صرافی‌ها
ندارد**. اگر LBank جواب نداد، سیستم به یاهو/gold-api برنمی‌گردد. دلیل: قیمت
طلای اسپات و قیمت پرپچوال طلا یکی نیستند؛ قاطی‌کردنشان یعنی سیگنال روی یک
قیمت و اجرا روی قیمت دیگر. برای برگشتن به مسیر قدیم:
```
METALS_DATA_SOURCE=legacy
```
هیچ خطی از مسیر یاهو/بریج پاک نشده، همه کامنت/`[LEGACY]` است.

---

## ۵) صفحه‌ی کاربران — ریسپانسیو شد

مشکل قبلی: یک جدول ۷ ستونی بدون هیچ حالت موبایلی، داخل `lg:grid-cols-3`.
روی گوشی هر ستون چند پیکسل می‌شد، دکمه‌های بازار ناخوانا و input‌های «کاربر
جدید» باریک‌تر از آن بودند که بتوانی تایپ کنی.

- زیر `md`: هر کاربر یک **کارت** جداگانه با برچسب‌گذاری واضح.
- از `md` به بالا: همان جدول برمی‌گردد (روی مانیتور جدول واقعاً بهتر است).
- فرم «کاربر جدید» تمام‌عرض؛ دکمه‌ها `w-full sm:w-auto`.
- مودال محدودیت ریسک: `max-h-[90vh] overflow-y-auto` و
  `grid-cols-1 sm:grid-cols-2` — قبلاً روی گوشی با کیبورد باز، دکمه‌ی ذخیره
  بیرون از دسترس بود.
- `min-w-0` و `break-all` روی سلول‌ها تا ایمیل بلند صفحه را افقی نکشد.
- جست‌وجوی کاربر بر اساس نام/ایمیل اضافه شد.

### یک باگ واقعی که پیدا و رفع شد
ترتیب هدرها این بود:
`username | email | active | admin | markets | risk | actions`
ولی سلول چهارم دکمه‌ی **ریسک** را رندر می‌کرد و سلول پنجم ستاره‌ی **ادمین** را.
یعنی دو ستون برچسب اشتباه داشتند. حالا سلول‌ها از هدرها تبعیت می‌کنند.

همچنین `is_superuser` به `_user_dict` اضافه شد تا در UI بین «ادمین» (★) و
«سوپرادمین» (★★) تفاوت دیده شود.

---

## ۶) رد شدن از حد ضرر → قطع اعلان معامله

### فایل جدید: `backend/apps/alerts/gating.py`
- `loss_limit_state(user)` → `{blocked, reason, lost, limit}`
  - اول `RiskLimits.daily_loss_limit_usdt` را با `today_realized_loss(user)`
    مقایسه می‌کند،
  - بعد `apps.strategy.risk.daily_risk_guard` را می‌پرسد (این تابع **تغییر
    نکرد**، فقط صدا زده می‌شود).
  - هیچ‌وقت exception پرت نمی‌کند؛ خرابی این تابع نباید کل مسیر اعلان را بخواباند.
- `TRADE_CATEGORIES = {"signal", "trading"}` — **فقط** این دو دسته خفه می‌شوند.
  هشدارهای ریسک، PnL و سیستمی همچنان می‌رسند: وقتی از حد ضرر رد شدی،
  «نرو معامله کن» را نباید ببینی، ولی «پوزیشنت به SL رسید» را باید.
- `notify_allowed(user, category)` در `apps/alerts/notify.py` قبل از هر ارسال
  چک می‌شود. یک `force=False` هم اضافه شد برای مواردی که باید رد شود.

### نکته‌ی ظریف در `apps/alerts/rules.py`
گیت **قبل از** `rules = list(...)` گذاشته شد، نه بعدش. اگر بعد از ساختن
قواعد می‌گذاشتم، `AlertRuleState.latched` عوض می‌شد و بعد از تمام‌شدن سکوت،
قاعده «فکر می‌کرد» قبلاً فایر شده و دیگر هیچ‌وقت اعلان نمی‌داد.
`stats` دو کلید جدید گرفت: `muted` و `muted_reason`.

---

## ۷) اعلان همکارت که برای تو می‌آمد — علت و رفع

**علت دقیق:** در `apps/alerts/push.py` تابع `broadcast(payload, users=None)`
وقتی `users` را `None` بگیرد، `PushSubscription.objects.filter(enabled=True)`
را **بدون فیلتر کاربر** می‌گیرد — یعنی برای هرکسی که پوش روشن کرده.
و `signal_check.py` آخر کارش می‌نوشت:
```python
return broadcast(payload)   # ← بدون users
```
پس هر سیگنال طلا برای همه می‌رفت، حتی اگر تو طلا را در واچ‌لیستت نگذاشته بودی.

**رفع:** `apps/alerts/signal_check.py` بازنویسی شد:
- `gating.watchers(symbol, score=…, timeframe=…)` فقط کاربرانی را برمی‌گرداند که
  یک `WatchItem` **فعال** منطبق با `{base, base:PERP, SYMBOL}` دارند **و**
  `min_score` شخصی‌شان برآورده شده. اگر کسی نبود، `[]` برمی‌گردد — یعنی هیچ‌کس.
- یک بار `gating.recipients()` صدا زده می‌شود و همان لیست هم به حلقه‌ی
  `notify(..., push=False)` و هم به `broadcast(payload, users=users)` می‌رود.
- `SignalState` (حافظه‌ی «این سیگنال را قبلاً دیدم») حالا **همیشه** آپدیت
  می‌شود، حتی اگر VAPID تنظیم نباشد. قبلاً یک `return` زودهنگام داشت که این
  حسابداری را رد می‌کرد.

### باگ دوم در همین مسیر
`apps/alerts/management/commands/monitor_scalp.py` یک لیست **هاردکد** داشت:
```python
WATCH = [("XAUUSD","5m"), ("XAGUSD","5m"), ("XAUUSD","15m")]
```
این یعنی طلا و نقره همیشه اسکن می‌شدند، بی‌ربط به واچ‌لیست کسی. کامنت شد و
جایش `pairs` فقط از `WatchItem`های فعال ساخته می‌شود. اگر واچ‌لیست فعالی نبود،
زودتر برمی‌گردد با پیام `"No active watchlist items — nothing to scan."`

---

## ۸) سوپرادمین: تغییر یوزر/پسورد و ریست دیتا

### بک‌اند — `backend/apps/accounts/views.py`
- `admin_user_detail` حالا **فقط برای سوپرادمین**:
  - تغییر **نام کاربری** (حداقل ۳ کاراکتر، یکتایی با `username__iexact` و
    بدون احتساب خودش)
  - تغییر **رمز عبور**
  - هر دو حالت، همه‌ی `ExpiringToken`های آن کاربر پاک می‌شود → فوراً از همه‌ی
    دستگاه‌ها خارج می‌شود. `log.warning` هم برای رد حسابرسی نوشته می‌شود.
- تابع جدید `admin_user_reset(request, user_id)`:
  - `IsAdminUser` + چک صریح `is_superuser`
  - **نمی‌تواند خودت را ریست کند** (جلوگیری از قفل‌شدن بیرون حساب خودت)
  - داخل `transaction.atomic` این‌ها پاک می‌شوند: `Trade`, `Notification`,
    `AlertRule`, `WatchItem`, `PushSubscription`, `MarketAccess`, `RiskLimits`,
    `ExpiringToken`
  - خودِ حساب می‌ماند و از صفر شروع می‌کند؛ خروجی تعداد رکوردهای پاک‌شده را
    برمی‌گرداند.
- `backend/apps/accounts/urls.py`: مسیر `users/<int:user_id>/reset/`

### فرانت — `frontend/src/pages/Admin.jsx`
کامپوننت `CredentialsEditor`:
- یوزرنیم و پسورد **دو دکمه‌ی جدا** دارند. یکی‌کردنشان یعنی وقتی داری پسورد
  ریست می‌کنی، ممکن است ناخواسته یوزرنیم را هم عوض کنی.
- ریست دیتا: باید **نام کاربری را تایپ کنی**. یک دیالوگ بله/خیر برای پاک‌کردن
  ژورنال معاملات یک نفر اصطکاک کافی نیست.
- در UI نوشته شده که تغییر یوزر/پسورد کاربر را از همه‌ی دستگاه‌ها بیرون می‌اندازد
  — تا سوپرایز نشود.

---

## ۹) لاگینگ و مشاهده‌پذیری (T-2)

### `backend/config/settings.py`
`LOGGING` تقریباً همان چیزی است که خودت پیشنهاد داده بودی، با یک تفاوت مهم:
`LOG_DIR` داخل `try/except OSError` ساخته می‌شود و اگر نشد `None` می‌ماند، و
`RotatingFileHandler` **فقط وقتی** `LOG_DIR` غیر `None` باشد اضافه می‌شود
(با `delay: True`).

**دلیل:** اگر پوشه‌ی `logs/` قابل نوشتن نباشد (پرمیشن اشتباه، فایل‌سیستم
read-only، یا کاربر gunicorn)، نسخه‌ی خام باعث می‌شود Django در لحظه‌ی import
کرش کند — یعنی «اضافه‌کردن لاگ» خودش کل سایت را می‌خواباند. حالا در بدترین
حالت فقط لاگ فایل نداری و کنسول کار می‌کند.

تنظیمات جدید: `LOG_DIR`, `LOG_LEVEL`, `LBANK_FUTURES_BASE_URL`, `METALS_DATA_SOURCE`

لاگرها: `smartpips.orders`, `smartpips.alerts`, `smartpips.market`,
`smartpips.market.lbank`, `smartpips.market.metals`, `smartpips.market.candles`,
`smartpips.health`, `smartpips.accounts`, `django.request` (ERROR),
`django.db.backends` (WARNING)

### فایل جدید: `backend/config/health.py`
| مسیر | دسترسی | کار |
|---|---|---|
| `GET /api/health/` | عمومی | `SELECT 1` + پینگ کش، `200`/`503`, `uptime_seconds` |
| `GET /api/health/deep/` | staff | وضعیت DB، کش، پوش (VAPID + تعداد subscription)، فید فلزات، حجم فایل لاگ |
| `GET /api/logs/` | **فقط سوپرادمین** | `lines` (پیش‌فرض ۲۰۰، سقف ۲۰۰۰)، `level`، `q` |
| `POST /api/logs/test/` | فقط سوپرادمین | یک warning و یک exception عمدی می‌نویسد تا مسیر لاگ را تست کنی |

دو نکته‌ی امنیتی که رعایت شد:
- `_SECRET_RE` قبل از خروج، api key / secret / token / password / authorization /
  bearer / VAPID private key / signature را ماسک می‌کند. پس می‌توانی یک خط لاگ
  را بدون نگرانی کپی کنی.
- فقط **۴ مگابایت آخر** فایل خوانده می‌شود (و خط ناقص اول دور انداخته می‌شود)،
  تا یک فایل لاگ چندگیگابایتی حافظه‌ی سرور را نبلعد.

### خطاهای خفه‌شده که صدادار شدند
- `apps/bitunix/views.py::_journal_open` → `log.exception(...)`
- `apps/mt5/views.py` (آن `except Exception: pass`) → `log.exception(...)`
- `apps/alerts/notify.py` (خطای mirror پوش) → `log.exception(...)`
- `apps/alerts/rules.py::current_price` → `log.exception(...)`
- `apps/market/metals.py::_from_ctrader` → `log.exception(...)`
- `apps/alerts/management/commands/monitor_scalp.py` (هر دو بلوک) → `log.exception(...)`

### فرانت — `frontend/src/pages/Logs.jsx` (جدید)
- مسیر `/app/logs`، در منو فقط برای ادمین.
- کارت‌های وضعیت سلامت بالای صفحه.
- فیلتر سطح (ERROR/WARNING/INFO)، تعداد خط، جست‌وجوی متنی.
- چون لاگ JSON-per-line است، هر خط parse و خوانا نمایش داده می‌شود؛ خطوطی که
  parse نشوند (ادامه‌ی traceback) خام و با رنگ سطح نشان داده می‌شوند.
- دکمه‌ی کپی همه، و رفرش خودکار ۱۰ ثانیه‌ای که **پیش‌فرض خاموش** است — لاگی که
  مدام می‌پرد وقت بررسی یک خطا غیرقابل‌خواندن است.

---

## ۱۰) کارهای باقی‌مانده برای تو

1. `backend/logs/` را به `.gitignore` اضافه کن.
2. در `config/settings.py` دو تنظیم `LBANK_FUTURES_BASE_URL` و
   `METALS_DATA_SOURCE` **دو بار** تعریف شده‌اند (حدود خط ۲۱۶/۲۲۰ و ۲۸۹/۲۹۳).
   مقدارشان یکسان است پس رفتار درست است (آخری برنده می‌شود)، ولی بهتر است
   یکی‌شان پاک شود.
3. **مهم:** درست‌بودن نام سیمبل و اندپوینت کندل LBank برای طلا/نقره در این
   سندباکس (بی‌اینترنت) قابل تست نبود. بعد از دیپلوی این را بزن:
   `GET /api/health/deep/` → کلید `metals_feed` را ببین. اگر `ok: false` بود،
   `/api/logs/?q=lbank` لاگ لاگر `smartpips.market.lbank` را نشان می‌دهد که
   کدام ترکیب اندپوینت امتحان و رد شده.
4. `TradeConfirmModal.jsx` تنها جای فعال بیت‌یونیکس است (بند ۱ را ببین) — بگو
   کامنتش کنم یا به LBank وصلش کنم.
5. `npm run build` را یک بار بزن. همه‌ی فایل‌ها parse می‌شوند و بک‌اند
   `compileall` تمیز رد می‌شود، ولی build کامل Vite در سندباکس اجرا نشد.

---

# نسخه ۲ — بازگردانی بیت‌یونیکس (۲۰۲۶-۰۹-۰۵)

## چرا این نسخه ساخته شد

در بسته قبلی بخشی از تغییرات **به‌درستی روی فایل‌ها ننشسته بود** (خطای من در ساخت بسته، نه خطای کد شما). نتیجه‌اش این سه ایراد بود که گزارش دادید:

| گزارش شما | علت واقعی |
|---|---|
| «نه می‌شه ریست بکنم پسورد رو نه هیچی» | سه تکه از زنجیره گم شده بود: متدهای `changeCredentials`/`resetUser` در `api/client.js`، مسیر `users/<id>/reset/` در `accounts/urls.py`، و خودِ ویو `admin_user_reset` در `accounts/views.py`. دکمه‌ها وجود داشتند ولی به هیچ‌جا وصل نبودند. |
| «اندیکاتورها اصلاً نیستش» | چارت بالای اسکالپ بدون پراپ `studies` رندر می‌شد، پس تریدینگ‌ویو خالی بالا می‌آمد. |
| «چارت پایینی هم دیتاش رفته» | `PositionChart` دیتای خود را از بیت‌یونیکس می‌گیرد و بیت‌یونیکس خاموش شده بود. |

## ۱) بیت‌یونیکس کامل برگشت

همه خطوطی که کامنت شده بود، دقیقاً به حالت قبل برگشت:

- `config/settings.py` → `BITUNIX_API_KEY` / `BITUNIX_SECRET_KEY` / `BITUNIX_BASE_URL` دوباره از `os.getenv` خوانده می‌شوند و `CRYPTO_PERP_DATA_SOURCE` برگشت به `bitunix`.
- `config/urls.py` → مسیر `api/bitunix/` فعال شد (کلید اصلی روشن/خاموش کردن).
- `apps/market/candles.py` → تابع `_bitunix_candles` و شاخه کریپتو فعال شد.
- `apps/market/quotes_resolver.py` → تابع `_bitunix_price` و هر دو نقطه فراخوانی فعال شد.
- `components/OrderBook.jsx` → عمق بازار برگشت به `bitunixApi.depth`.
- `pages/Dashboard.jsx` → ایمپورت و پنل `PositionChart` از کامنت درآمد. **(چارت پایینی درست شد)**
- `pages/Scalp.jsx` → پر شدن خودکار موجودی کیف پول از بیت‌یونیکس برگشت.
- `components/BitunixMiniPanel.jsx` → بنر «غیرقابل‌دسترس» حذف شد.

**یک نکته فنی مهم:** در `candles.py` شاخه فلزات، `settings` را داخل بلوک `try` خودش ایمپورت می‌کند، پس در شاخه کریپتو در دسترس نبود. اگر فقط کامنت را برمی‌داشتم، `NameError` می‌گرفتید. داخل شاخه کریپتو ایمپورت جدا اضافه شد.

## ۲) طلا و نقره همان LBank ماند

هیچ دست نخورد: `apps/lbank/metals.py`، `METALS_DATA_SOURCE="lbank"` و شاخه فلزات همه سرجایشان هستند. حالا دو صرافی هم‌پوشانی ندارند: **فلزات از LBank، کریپتو از بیت‌یونیکس.**

## ۳) بخش کاربران درست شد (این بود که کار نمی‌کرد)

- `adminApi.changeCredentials(id, {username, password})` اضافه شد — فقط فیلدی که پر کرده‌اید را می‌فرستد، پس ذخیره یوزرنیم هرگز پسورد را خالی نمی‌کند.
- `adminApi.resetUser(id)` اضافه شد.
- مسیر `users/<int:user_id>/reset/` ثبت شد.
- ویو `admin_user_reset` بازنویسی شد: داخل `transaction.atomic()` معاملات، اعلان‌ها، قوانین هشدار، واچ‌لیست، ساب‌اسکریپشن‌های پوش، دسترسی بازار، حد ضرر و توکن‌ها را پاک می‌کند. **خود اکانت باقی می‌ماند** (یوزرنیم و پسورد همان).
- تغییر یوزرنیم و پسورد **فقط برای سوپر ادمین**؛ ادمین معمولی ۴۰۳ می‌گیرد.
- بعد از تغییر یوزرنیم یا پسورد، توکن‌ها پاک می‌شوند تا دستگاه‌های قبلی مجبور به لاگین مجدد شوند. بدون این، توکن قدیمی کار می‌کرد و ریست پسورد فقط ظاهری بود.
- چک تکراری‌بودن یوزرنیم با `iexact` است، چون یوزرنیم جنگو به بزرگی/کوچکی حرف حساس است و وگرنه «Ali» و «ali» کنار هم ساخته می‌شد.

## ۴) اندیکاتورها روی چارت بالا برگشت

`SCALP_STUDIES` با `indicatorsToStudies` ساخته شد (EMA، RSI، MACD، Stochastic) و به `<SignalChart>` پاس داده شد.

**چرا لیست ثابت؟** `lib/scalp.js` اندیکاتورها را با **برچسب انسانی** («EMA 9»، «Stoch %K/%D») بیرون می‌دهد که به شناسه‌های تریدینگ‌ویو نمی‌خورد. این const سطح ماژول است، نه داخل رندر؛ آرایه جدید در هر رندر باعث می‌شد آی‌فریم چارت با هر تیک از نو ساخته شود.

## ۵) اعلان‌ها دست نخورد

طبق درخواست شما. `gating.py` و `signal_check.py` سرجایشان هستند، پس **رفع نشتی اعلان بین کاربران فعال است**.

ولی صادقانه: دو تغییر جانبی اعلان‌ها در همان اتفاق قبلی گم شد و **عمداً برنگرداندم** چون گفتید دست نزنم:

1. قلاب‌های «سکوت پس از رد شدن از حد ضرر» در `alerts/notify.py` و `alerts/rules.py`.
2. واچ‌لیست‌محور شدن `monitor_scalp.py` (الان همان `WATCH` ثابت است).

هر وقت بگویید این دو را برمی‌گردانم.


---

# نسخه ۳ — بازگردانی موارد باقی‌مانده + نوار چسبان معاملات (۲۰۲۶-۰۹-۰۶)

همهٔ تغییرات تاییدشدهٔ نسخهٔ ۲ سرِ جای خودشان هستند (بیت‌یونیکس کامل، طلا/نقره از LBank،
بخش کاربران، اندیکاتورها). قبل از بسته‌بندی، کل درخت با نسخهٔ دست‌نخوردهٔ اصلی `diff` شد
و فقط همین فایل‌های موردنظر تفاوت داشتند.

## ۱) سکوت اعلان‌ها پس از رد شدن از حد ضرر

**`apps/alerts/notify.py`**

- پارامتر `force=False` اضافه شد و پیش از ساختن `Notification`، تابع `notify_allowed`
  از `gating.py` صدا زده می‌شود.
- فقط دسته‌های معاملاتی (`signal`, `trading`) سکوت می‌شوند. دسته‌های
  `risk` / `pnl` / `system` **همیشه** رد می‌شوند — چون خفه‌کردنِ آن‌ها دقیقاً همان
  هشداری را پنهان می‌کند که توضیح می‌دهد چرا سیگنال‌ها قطع شده‌اند.
- `force=True` برای پیام‌هایی است که باید در هر شرایطی برسند (مثل خودِ پیامِ
  «به حد ضرر رسیدی»).
- اگر خودِ گیت خطا بدهد، خطا لاگ می‌شود و پیام **ارسال** می‌شود؛ یک باگ در گیت نباید
  کل اعلان‌ها را بی‌صدا از بین ببرد.
- `except Exception: pass` در مسیر پوش به `log.exception` تبدیل شد.

**`apps/alerts/rules.py`**

- بررسی حد ضرر **قبل از حلقه** انجام می‌شود، نه با تکیه بر `notify()`.
  دلیل دقیق: اگر قوانین در حالت سکوت ارزیابی شوند، `AlertRuleState` قفل (latch) می‌کند و
  فردا که حد ضرر ریست شد، شرطی که هنوز برقرار است «قبلاً گزارش شده» حساب می‌شود و
  ساکت می‌ماند تا زمانی که برقرار نبودن و دوباره برقرار شدن رخ دهد.
- کلیدهای `muted` و `muted_reason` به خروجی آمار اضافه شد تا «اعلان‌هایم قطع شده»
  یک دلیل قابل‌مشاهده داشته باشد و شبیه کرانِ خراب به نظر نرسد.

## ۲) `monitor_scalp.py` — واچ‌لیست‌محور و قابل‌مشاهده

نکته: خواندن واچ‌لیست کاربران از قبل در این فایل وجود داشت (خطوط ۳۲ تا ۴۲) و گم نشده بود.
آنچه اضافه شد:

- لاگر + `log.exception` برای خطای بارگذاری واچ‌لیست و خطای هر پاس.
- تعداد ناظرانِ هر نماد در خروجی چاپ می‌شود، چون در خروجی قبلی «سیگنال جدیدی نیست» و
  «هیچ‌کس این نماد را واچ نکرده» یکسان دیده می‌شدند در حالی که راه‌حلشان کاملاً متفاوت است.
- یک خط جمع‌بندی در پایان.
- **عمداً** نمادهای بدون ناظر هم ارزیابی می‌شوند: `evaluate_and_alert` وضعیت `SignalState`
  را به‌روز نگه می‌دارد و اگر رد می‌شدیم، اولین پاس پس از افزودن واچ روی یک تغییرِ کهنه
  شلیک می‌کرد. (تحویلِ اعلان همچنان با `gating.py` محدود به ناظرانِ همان نماد است.)

## ۳) `log.exception` در مسیرهای بی‌صدا

| فایل | محل | چرا خطرناک بود |
|---|---|---|
| `apps/mt5/views.py` | بعد از `bridge_push` | EA کندل می‌فرستاد و `200 OK` می‌گرفت در حالی که هشدارها مرده بود |
| `apps/bitunix/views.py` | `_journal_open` | بدترین حالت: پوزیشن واقعی باز بود ولی هیچ ردیفی در ژورنال نبود؛ صفحهٔ معاملات، محاسبهٔ سود/زیان و حد ضرر روزانه همه کم‌شمار می‌شدند |
| `apps/bitunix/views.py` | `_journal_close` | معاملهٔ بسته‌شده در ژورنال «باز» می‌ماند و بودجهٔ مارجین را می‌خورد |
| `apps/bitunix/views.py` | پیش‌بررسی حد ریسک | همچنان سفارش را بلاک نمی‌کند، ولی حالا دیده می‌شود که کلید محافظ کار نمی‌کند |
| `apps/bitunix/views.py` | بستنِ سریع (flash close) | به‌روزرسانی ژورنال بی‌صدا شکست می‌خورد |

لاگر `smartpips.orders` از قبل در `settings.LOGGING` روی `INFO` تنظیم شده بود.

## ۴) نوار چسبان معاملات باز (صفحهٔ اسکالپ)

کامپوننت `OpenTradesBar` در `frontend/src/pages/Scalp.jsx`:

- اگر معاملهٔ بازی نباشد **هیچ چیز رندر نمی‌کند** و فضایی نمی‌گیرد.
- به پایین صفحه چسبیده است؛ با تپ کردن باز و بسته می‌شود.
- هر ۲۰ ثانیه (همان ریتم بقیهٔ صفحه) و با هر ثبت معاملهٔ جدید (`tradesVersion`) به‌روز می‌شود.
- **جای‌گیری:** کلاس `.trades-bar-offset` در `index.css` با
  `calc(3.5rem + env(safe-area-inset-bottom))`. یک `bottom-14` سادهٔ Tailwind غلط بود،
  چون نوار پایینی موبایل در `Layout.jsx` کلاس `.tabbar-safe` دارد
  (`padding-bottom: env(safe-area-inset-bottom)`) و روی آیفون‌های نوچ‌دار،
  نوار زیر نشانگر خانه (home indicator) می‌افتاد.
- `pb-28 md:pb-24` **فقط وقتی** به کانتینر اسکرول اضافه می‌شود که نوار واقعاً دیده شود.
- **ریاضی سود/زیان:** `ROI = درصد حرکت قیمت × ضریب اهرم`. این رابطه مقیاس‌ناپذیر است و
  برای طلا و کریپتو یکسان درست کار می‌کند، بدون نیاز به `CONTRACT_SIZE` یا قرارداد
  «size = مارجین».
- برای معاملاتی که قیمت زندهٔ نمادشان را نداریم `—` نشان داده می‌شود، نه `0%`
  (صفر یعنی سربه‌سر و گمراه‌کننده است).
- عدد بالای نوار **میانگین** ROI است نه جمع؛ جمع‌کردن درصدهای پوزیشن‌هایی با اندازهٔ
  متفاوت عددی بی‌معنا تولید می‌کند.
- رنگ‌ها از توکن‌های خودِ پروژه‌اند (`up`, `down`, `gold`, `ink`, `mist`).
  `emerald` / `rose` / `brand` استفاده **نشد**: `brand` در `tailwind.config.js` وجود ندارد
  و `mist` تا ۵۰۰ است، پس آن کلاس‌ها بی‌صدا بی‌رنگ رندر می‌شدند.

## آنچه تست شد و آنچه تست نشد

- `python3 -m compileall backend/apps backend/config` → موفق.
- تحلیل سینتکس تک‌تک فایل‌های `.js/.jsx` و `index.css` → موفق.
- `diff -rq` با نسخهٔ اصلی → فقط فایل‌های موردنظر.
- **اجرا نشد:** `npm run build` و هر رفتار زمان‌اجرا. سندباکس نه اینترنت دارد نه `django`
  و نه `node_modules`. صحتِ داده‌ها فقط روی سرور خودتان قابل تایید است.
